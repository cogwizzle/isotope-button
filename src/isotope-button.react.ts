import React from 'react';
import { createComponent } from '@lit-labs/react';
import { IsotopeButton as Button } from './index.js';
import './isotope-button.js';

export const IsotopeButton = createComponent(React, 'isotope-button', Button);
